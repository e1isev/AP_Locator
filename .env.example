# App runtime (used by the Expo app via app.json -> extra)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Seed script only (service role key, never bundle this into the app)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
